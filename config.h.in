#pragma once

#define EMAP_PACKAGE "${PROJECT_NAME}"
#define EMAP_VERSION "${PROJECT_VERSION_MAJOR}.${PROJECT_VERSION_MINOR}.${PROJECT_VERSION_PATCH}"
#define EMAP_COMMIT_HASH "${PACKAGE_VERSION_COMMITHASH}"
